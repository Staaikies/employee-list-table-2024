/* == QUestionable Code == */


const Button = (Props) => {
    return (
        <button className={`button  ` + Props.style}></button>
    )
}

const Heading = (Props) => {
    return (
        
    )
}